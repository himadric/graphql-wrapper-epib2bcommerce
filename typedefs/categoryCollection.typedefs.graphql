type CategoryCollection {
    uri: String,
    categories: [Category]
}

type Query {
    categoryCollection(maxdepth: Int, startCategoryId: String, includeStartCategory: Boolean): CategoryCollection
}